# Hail 项目协作规则

## 项目概况
- 本项目是 Kotlin 编写的 Android 应用，应用 ID 为 `com.aistra.hail`。
- 冻结能力包含强行停止、停用、隐藏、暂停，多种工作模式的权限边界不同。

## 修改约束
- 保持最小改动，不混合无关重构或依赖升级。
- 涉及应用启动流程时，同时核对应用内入口 `PagerFragment.launchApp` 与 API/快捷方式入口 `ApiActivity.launchApp`，避免行为不一致。
- 涉及冻结实现时，区分 Root、Shizuku、设备所有者、Dhizuku、Island/Insular 模式，不假设它们拥有相同权限。
- 不用吞异常或伪造成功规避冻结、解冻失败；失败必须可见并保留诊断信息。
- 未经用户明确同意，不执行 Gradle 编译、测试或打包。

## 验证要求
- 修改后至少进行静态检查：调用链、空值路径、Android API 版本兼容性及各工作模式影响。
- 需要设备验证的 ROM 私有行为，应明确设备、系统版本、工作模式和复现步骤，不将静态推断表述为真机验证结果。
